# InuSDK's bucket repository

This is where InuSDK retrieve data to install your desired SDK in your machine.

## Scripts

Right now we have three scripts to generate the manifest, for Java, go and node.js
